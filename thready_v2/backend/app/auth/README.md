# Auth

- email.py     — register, login, reset password, JWT
- google.py    — Google OAuth2 via authlib
- github.py    — GitHub OAuth2 via authlib
- session.py   — session middleware, current_user dependency
